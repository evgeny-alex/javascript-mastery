const lessonContent = {
  title: "Math object",
  content: `
    <p>Built-in Math object: common methods like Math.round, Math.floor, Math.ceil, Math.sqrt, Math.pow.</p>
    <ul>
      <li>Examples and use cases</li>
      <li>Working with floating point numbers</li>
    </ul>
  `,
};

export default lessonContent;
